/*
  NodeOCR.start(filePath, options) returns a Promise
  options: {
    language,
    textcleaner
  }
*/

class NodeOCR {
  start(filePath, options) {
    return new Promise((resolve, reject) => {

    });
  }
}

export default new NodeOCR();
